🧪 Тестовое задание: Сервис уведомлений через Telegram-бота

📌 Цель:
Создать небольшой сервис на Laravel, который получает задачи из внешнего API и рассылает уведомления пользователям в Telegram.

---

🔧 Требования:

Backend:
- Laravel (любая версия 9–12)
- Чистый код, соблюдение принципов SOLID
- Работа с внешним API
- Telegram Bot API (использовать curl или Guzzle, не использовать SDK-библиотеки)
- Использование очередей (Laravel queues)
- Миграции, сидеры

Задача:
1. Создать таблицу `users`:
   - `id`
   - `name`
   - `telegram_id` (уникальный)
   - `subscribed` (boolean)

2. Реализовать Telegram-бота:
   - Команда `/start` — сохраняет пользователя в БД.
   - Команда `/stop` — меняет флаг `subscribed` на `false`.

3. Создать консольную команду `NotifyTasks`, которая:
   - Запрашивает список задач из внешнего API (например, https://jsonplaceholder.typicode.com/todos).
   - Выбирает задачи, где `completed = false` и `userId <= 5`.
   - Для каждого активного пользователя (`subscribed = true`) отправляет список задач в Telegram.

4. Уведомления в Telegram должны выполняться через очередь.

---

✅ Бонусные требования:
- Написать минимальные тесты (на 1–2 кейса)
- Swagger-документация или Postman-коллекция
- Docker-окружение

---

📤 Что предоставить:
- Ссылка на GitHub-репозиторий
- Краткий README с инструкцией запуска
- Пример запроса к внешнему API и ответа в Telegram